<?xml version="1.0" encoding="UTF-8"?>
<project name="Project Install Targets" default="help">
	<target name="help">
		<echo>Target should not be called directly</echo>
	</target>
	
	<target name="before">
		<!-- custom project actions _before_ install -->
	</target>
	
	<target name="after">
		<!-- custom project actions _after_ install -->
	</target>
</project>