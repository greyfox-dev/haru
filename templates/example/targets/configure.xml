<?xml version="1.0" encoding="UTF-8"?>
<project name="Project Configure Targets" default="help">
	<target name="help">
		<echo>Target should not be called directly</echo>
	</target>
	
	<target name="before">
		<!-- custom project actions _before_ configure -->
	</target>
	
	<target name="after">
		<!-- custom project actions _after_ configure -->		
	</target>
</project>